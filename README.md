# Git Cheat Sheet

```
code .
npm start
git status
git add . && git commit -m 'Boilerplate start for react app'
gh create repo
gh repo create
git checkout -b feature/header
git branch
git status
git add . && git commit -m 'Add content to home page'
git push origin feature/header
git checkout main
git pull origin main
git branch
git branch -D feature/header
```
